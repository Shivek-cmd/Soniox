# ─── Voice Bot Server (server/) ───────────────────────────────
# These are the actual deps from soniox-voice-bot-demo/server/pyproject.toml
# Requires Python >= 3.13
openai>=1.101.0          # LLM (can swap for anthropic later)
python-dotenv>=1.1.1     # load .env file
structlog>=25.4.0        # logging
websockets>=15.0.1       # WebSocket connection to Soniox STT/TTS
silero-vad               # Voice Activity Detection model
torch                    # required by silero-vad
torchaudio               # required by silero-vad
onnxruntime              # required by silero-vad (faster inference)
numpy                    # audio processing
pydantic>=2.11.7         # request validation

# ─── Twilio Bridge (twilio/) ──────────────────────────────────
# These are the actual deps from soniox-voice-bot-demo/twilio/pyproject.toml
audioop-lts>=0.2.2       # audio format conversion (mulaw ↔ PCM) — Python 3.13 compatible
fastapi>=0.116.1         # web server for Twilio webhook
twilio>=9.7.2            # Twilio helper library
uvicorn>=0.35.0          # run FastAPI server

# ─── Install with uv (recommended) ───────────────────────────
# uv venv && uv sync
# DO NOT use pip install -r requirements.txt for these —
# each folder has its own pyproject.toml, run uv sync inside each folder
